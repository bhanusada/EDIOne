import { Component } from '@angular/core';

@Component({
    selector: 'sample-content',
    template: `<div>
    <label>Example of custom component used as tab content</label>
    <button class="button primary md">Button</button>
    </div>`
})
export class SampleContent { }
